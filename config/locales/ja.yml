ja: #エラーメッセージの日本語化用
  activerecord:
    attributes: # attributes:の直下に
      cabinet: # モデル名を指定し
        file_no: ファイルNo # カラム名を指定する。
        file_name: ファイル名 # カラム名を指定する。
        expired_at: 保管期限 # カラム名を指定する。
        placed_at: 保管場所 # カラム名を指定する。


ja:
  date:
    formats:
      default: "%Y/%m/%d"
      
ja: #既定
  views: #フォルダ名
    pagination: #既定？
      first: "&laquo; 最初"
      last: "最後 &raquo;"
      previous: "&lsaquo; 前"
      next: "次 &rsaquo;"
      truncate: "..."